#include <stdio.h>
#include <stdlib.h>

int main()
{
    // Declracao de variaveis
    int i, j, p, q, x, y, contX, contY;

    // Lendo
    scanf ("%d%d%d%d",&p,&q,&x,&y);

    // Delimitando quadrado
    if (x >= p && (x - p) <= 99)
    {
        contX = 1;
    }
    if (y >= q && (y - q) <= 99)
    {
        contY
    }
    printf("No");
printf("Yes");



    return 0;
}
