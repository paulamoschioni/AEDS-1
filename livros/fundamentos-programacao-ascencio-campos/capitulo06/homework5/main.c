#include <stdio.h>
#include <stdlib.h>

int main()
{
    // Declaracao de variaveis
    int vet1[10], vet2[10], verR[];

    // Preenchendo vetores
    for (int i = 0; i < 10; i++){
        scanf("%d",&vet1[i]);
    }
    for (int j = 0; j < 10; j++){
        scanf("%d",&vet2[j]);
    }

    // Uniao entre os dois vetores
    for(int k = 0; k < 10; k++) {   /// Percorrendo todas as posicoes do vetor 1 e gravando em vR
    while (*(vet1 + k) != )         /// Preciso que TODAS as posicoes do vetor sejam diferentes ás anteriores


    }



    return 0;
}
